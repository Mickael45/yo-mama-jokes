# SEO / GEO audit — Yo Mama Jokes Central — 2026-07-18

- **Site:** yo-mama-jokes (https://yomamajokescentral.com)
- **Stack:** Astro 6 static SSG → Cloudflare Workers (static assets + thin Telegram-webhook Worker)
- **Ad status:** ad-free — Google AdSense + GA4 + cookie banner removed (commit `edbb8f8`). Ad-network and consent scopes therefore N/A.
- **Locales:** single-language `en-US`.
- **Audited target:** working tree committed this run (prior HEAD `d472d6c`).
- **Pages audited:** 28 (home, about, categories, contact, terms, privacy-policy, 404, + 21 category pages).
- **Prior audit doc:** none (`SEO-AUDIT.md` created this run). `SEO_COOKBOOK.md` is the site's build doctrine, not a run record.

## Result

Mechanically strong site. **3 findings fixed in the same commit as this report** (so the audited sha is clean); 0 open. Findings were 1× honesty (fake "daily" freshness — 🟠 high), 1× metadata (short `/about` title), 1× hygiene (Bing placeholder token in prod). Full disposition below.

### Fixed this run

| # | Sev | Finding | Evidence | Fix |
|---|-----|---------|----------|-----|
| 1 | 🟠 high | **Fake-freshness claim.** "refreshed daily / every single day / featured jokes of the day" asserted in 6 spots, but the daily-rebuild workflow was dropped (`a1727ea`) and the featured set is build-time date-seeded — so "daily" is no longer guaranteed (rebuilds now fire only on irregular comedy-bot commits). Violates honest-signals rule. | `src/pages/index.astro:25,36,40,42,56,84`; `src/pages/about.astro:23`; `public/llms.txt:5,11`; `lib/siteConfig.ts:9` | Softened all copy + schema (`"Featured Jokes"`, `"rotating selection"`, `"regularly rotate"`) to honest, non-guaranteeing language. |
| 2 | 🟢 low | **Boilerplate-short `<title>`** on `/about` — "About \| Yo Mama Jokes Central" (29 chars). | `src/pages/about.astro:5` | → "About Yo Mama Jokes Central — Who We Are & How We Curate" (~58 chars). |
| 3 | 🟡 med | **Placeholder Bing token in prod** — `<meta name="msvalidate.01" content="REPLACE_WITH_BING_TOKEN">` shipped on every page. | `layouts/Layout.astro:33` | Removed the placeholder meta; left a comment reminder to add the real token post-verification. |

Also fixed (doc hygiene, not a registry check): stale `astro.config.mjs:11` comment claiming "Canonical host = www" — corrected to apex (www 301→apex), matching `lib/siteConfig.ts` and reality.

## Checks run

### raw-html-content
PASS. Category pages ship all joke text + authored editorial prose in raw HTML (no JS gating). `dist/jokes/fat-yo-mama-jokes/index.html` renders 5 joke cards + `<section class="site-answer">` server-side.

### robots-txt-valid
PASS. `/robots.txt` valid, `Allow: /`, sitemap declared.

### robots-prod-vs-repo
PASS. Prod `robots.txt` byte-identical to `public/robots.txt`; crawler-UA curl (GPTBot) returns the same file — no CDN-injected managed/Content-Signals block.

### sitemap-valid
PASS. `sitemap-index.xml` → `sitemap-0.xml`, 27 `<loc>`s, all trailing-slash-free (matches `trailingSlash: "never"`). Prod spot-checks (`/`, `/sitemap-index.xml`, `/jokes/fat-yo-mama-jokes`) → 200.

### canonical-present
PASS. Single self-referencing absolute canonical per page via `<Seo>` (`components/Seo.astro:40`).

### canonical-resolves-200
PASS. Canonicals target apex; `https://yomamajokescentral.com/` → 200 (not a 3xx).

### host-canonicalization
PASS (by name). `http://apex`→301 https apex; `https://apex`→200; `http://www`→301 https www→301 apex; `https://www`→301 apex. All resolve to the apex canonical.

### public-dns-resolution
PASS. apex + www both resolve A (104.21.82.56 / 172.67.196.20) and AAAA from @1.1.1.1 and @8.8.8.8 — no empty-A / black-hole-AAAA.

### indexability-directives
PASS. `<meta robots>` = `index, follow, max-image-preview:large` sitewide; `noindex` only when a page passes `noindex` (none do). No accidental noindex regression since `d472d6c`.

### soft-404-real-status
PASS. `not_found_handling: "404-page"` in `wrangler.jsonc`; prod `/this-does-not-exist-xyz` → real HTTP 404.

### lastmod-honest
PASS. Sitemap `lastmod` derived per-URL (`scripts/lastmod.mjs`), omitted when unknown; values span 2025-04 → 2026-07 (non-flat, honest).

### title-unique
PASS. All 28 titles unique.

### title-length
FIXED (finding #2). `/about` was 29 chars (boilerplate-only). Now ~58 chars. All other titles 30–69 chars.

### meta-description-unique
PASS. Descriptions unique; category descriptions are distinct prose, not templated near-duplicates.

### meta-description-length
PASS. Sampled descriptions 106–157 chars — within the effective range.

### single-h1
PASS. Exactly one `<h1>` per page (layout has none; each page supplies its own).

### html-lang
PASS. `<html lang="en">` (`layouts/Layout.astro:22`).

### viewport-meta
PASS. `width=device-width, initial-scale=1`.

### schema-valid
PASS. Every page ships parseable JSON-LD; category page = BreadcrumbList + CollectionPage + FAQPage + [Organization, WebSite]. All parse.

### schema-matches-content
FIXED. Home schema previously named the collection "Featured Jokes of the Day" / "A daily selection…", reinforcing the unguaranteed daily cadence (finding #1). Softened to "Featured Jokes" / "rotating selection". Remaining schema (categories, breadcrumbs, org) matches visible content; `dateModified` mirrors the visible "Last updated".

### og-image-present
PASS. `og:image` = `/og/default.png` (or per-category PNG) — raster PNG, not SVG; 1200×630 declared.

### open-graph-complete
PASS. Full OG set (type, site_name, title, description, url, image + dims + alt, locale) + Twitter `summary_large_image`. `twitter:site` omitted (no handle yet) — acceptable.

### favicon-icon-set
PASS. `favicon.ico`, `favicon.svg`, `apple-touch-icon.png`, manifest icons 192/512/maskable.

### manifest-installable
PASS. `site.webmanifest`: `display: standalone`, icons 192 + 512 + maskable-512, start_url `/`, theme/background set. Service worker (`sw.js`, full-offline precache) registered prod-only.

### core-web-vitals
PASS (lab-observable). Self-hosted fonts (no render-blocking Google Fonts), preloaded display font with size-matched fallback, content-hashed `_astro/*` immutable-cached, minimal inline JS, no client framework hydration. Field CWV pending ~28d CrUX (handover).

### security-headers
PASS. HSTS (`max-age=63072000; includeSubDomains; preload`), `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin` served on `/*`. No CSP — optional for a static ad-free site; not a finding.

### ai-crawler-access
PASS. `robots.txt` explicitly allows GPTBot, OAI-SearchBot, ChatGPT-User, PerplexityBot, ClaudeBot, Google-Extended. No CDN-level training/managed block (verified via crawler-UA curl). `llms.txt` present (bonus, not required).

### publisher-identity-visible
PASS. Real brand "Yo Mama Jokes Central" in header, footer copyright, About page, and Organization schema (with support email). Not a placeholder/joke publisher name.

### eeat-identity
PASS. About page states who/how-we-curate; 21 categories carry original authored prose (`lib/categoryContent.ts`) with a question, direct answer, body paragraphs, and real outbound citations (e.g. Britannica "the dozens").

### content-spam-guardrails
PASS. Single standalone site; no doorway pages, no site-network cross-linking, no scaled/auto-published boilerplate — category pages carry hand-authored editorial. Comedy-bot appends are human-approved via Telegram before commit.

### orphaned-pages
PASS. All 27 sitemap URLs reachable from `/`: header/footer nav → home, categories, about, contact, privacy, terms; `/categories` links every joke category; home body links several categories.

### click-depth-exceeded
PASS. Max depth 2 (home → /categories → /jokes/{cat}); legal/about pages depth 1 from nav.

## Checks skipped (with reason)

- **hreflang-valid** — single-language site (`en-US`); no alternate locales.
- **ads-txt-valid** — ad-free; AdSense removed (`edbb8f8`). No ads.txt expected.
- **ad-unit-renders** — ad-free; no ad loader present.
- **publisher-id-correct** — ad-free; no ad publisher ID present.
- **cmp-consent-footer** — no CMP ships (no ads, no consent-requiring analytics).
- **consent-mode-region-scoped** — no Consent Mode / GA / ad tags present.

## Handover (human/time-only)

- **Field CWV** — needs ~28 days CrUX/GSC data post-change.
- **AI-citation movement** — needs the monthly fixed-prompt GEO audit over time.
- **Bing verification** — add the real `msvalidate.01` token in `layouts/Layout.astro` after verifying at bing.com/webmasters (unblocks Copilot).
- **Restore true daily freshness (optional)** — if the owner wants the "daily" hook back, add a scheduled GitHub Actions cron hitting a Cloudflare Deploy Hook (needs a `CLOUDFLARE_DEPLOY_HOOK_URL` secret). Deferred by owner decision this run in favour of honest copy.
- **`SAME_AS` / social profiles** — empty; populate `lib/siteConfig.ts` `SAME_AS` as accounts are created (entity disambiguation / GEO).

## Ceiling

Passes all automated and agent-executable checks; field data and rankings pending.

Sidecar: `audits/seo/latest.json` (validated against the skill registry).

## 2026-07-21 — HSTS raised to rev-2 baseline

security-headers: `Strict-Transport-Security` now `max-age=31536000; includeSubDomains` (preload off) live, up from `max-age=15552000` (180d) without includeSubDomains — meets the rev-2 baseline. Set via the SSL/TLS → Edge Certificates HSTS toggle; verified with `curl -sSI`.
